# 찍 · 구현 및 검증 상태

최종 확인: 2026-09-17. 아래 단계별 기록은 당시 실행 결과다. 최신 진행 지점은 맨 아래 **AWS 연결 준비 3단계**이며, 이번 후속 작업은 로컬 Chrome을 사용하지 않는다.

## 이어받은 상태와 이번 구현

기존 작업에는 DB 모델·이미지 처리·API 계약·업로드/인물 UI 일부만 있었다. 메인 앱, 스타일, API 실행 모듈, worker, seed, 마이그레이션 및 검증이 빠져 앱이 실행되지 않았다. 기존 코드와 참고 이미지 2개를 보존하며 아래 기능을 연결했다. 서비스 이름은 최신 사용자 지시인 **찍**으로 수정했다. 최초 요청 문서 `moacut_codex_prompt.md`는 원문이므로 수정하지 않았다. 기존 개발용 DB·세션·샘플 계정 식별자는 저장 데이터와 호환되도록 유지한다.

| 영역 | 상태 | 구현과 검증 범위 |
| --- | --- | --- |
| 화면·디자인 | 구현·검증 완료 | 두 참고 이미지 기반 재구성. PC 파란색 사이드바·4열 사진·우측 정보, 모바일 코랄색 앨범·2열 사진·큰 인물 필터·하단 메뉴. 장식 문구/배너/과도한 여백 제거, 찍 로고/제목/파비콘 통일 |
| 인증 | 구현·검증 완료 | Argon2 비밀번호, 만료 세션·HttpOnly 쿠키·CSRF·Origin 검사. 지수/민지 독립 세션으로 실제 로그인 |
| 앨범·초대 | 구현·검증 완료 | 생성/참여/목록/상세, 이름·소개·여행 시간대 수정, 초대코드 재발급, 멤버 추방·탈퇴·앨범 삭제 화면과 서버 권한. 생성/삭제/탈퇴 후 캐시·화면 전환 검증 |
| 기준 인물·계정 | 구현·검증 완료 | 기준 사진 검증, 제안받은 본인의 연결 수락, 연결 해제·삭제, 다른 앨범 참조 차단. 얼굴 없음/여러 얼굴 오류 분리 |
| 업로드 | 구현·검증 완료 | JPEG/PNG 실제 디코딩·MIME·25MB·픽셀 제한. 미리보기/제외/3개 동시 전송/실패 재시도/전송 중단·재개. 요청 ID 중복 방지, 부분 저장 실패 정리 |
| 영속 분석 worker | 구현·검증 완료 | DB 작업·lease·heartbeat·작업 토큰·제한 재시도·중단 복구. 상태별 장수·실패 원인·실행 기록 화면, 실패 일괄 재분석. 활성 lease 보존과 동시 재요청 중복 방지 |
| fixture 분석 | 샘플 전용·검증 완료 | 지정 파일의 실제 SHA-256 일치만 성공. 임의 사진은 분석 실패, 저장된 파일은 보존. API/화면 샘플 표시 |
| 등록 인물 분석 | 코드 구현/실연동 미검증 | Rekognition DetectFaces + CompareFaces, 얼굴 위치 대응, 후보 경합·임계값, 앨범 격리. 모의 응답만 검증 |
| 필터·검색 | 구현·검증 완료 | 내 사진/인물/1인/2인 이상/미검출/확인 필요/최종본, 인물 AND/OR, 태그·촬영일·정렬·페이지네이션. 내 사진과 혼자/함께 조건 조합 |
| 수동 인물 수정 | 구현·검증 완료 | 인물 추가·제외 저장, 재분석 후에도 유지, 승인 대상 변경 시 재검토 |
| 보정·버전 | 구현·검증 완료 | 서버 밝기·채도 미리보기, debounce·이전 요청 취소, 원본 비교, 부모 버전 분기, 작성자·시간·설정·렌더러 기록 |
| 렌더·다운로드 | 구현·검증 완료 | EXIF 방향 통일, sRGB, 밝기→채도 순서, 매번 원본 렌더, 버전 캐시. 원본/선택/최종 다운로드 및 선택 원본 ZIP |
| 승인·최종본 | 구현·검증 완료 | 사람이 대상 확인, 버전별 불변 대상, 승인 0개로 시작, 중복 방지·취소, 전원 합의 후 최종본 하나. 인물/계정/멤버 변경 시 재검토 및 최종 해제 |
| 협업 | 구현·검증 완료 | 버전 댓글·수정 요청·버전 이름, 사진 메모·목적, 선택/보정/확인/최종 보드, 저장과 분리한 앱 알림 |
| 촬영 정보 | 구현·검증 완료 | EXIF 촬영 시각·시간대·GPS 추출. 시간대 없는 값을 임의로 확정하지 않음. 샘플의 없는 촬영/장소 정보는 명시 |
| 장면·구체적 장소 | 코드 구현/실연동 미검증 | Rekognition 장면 라벨 + GPS 있을 때만 설정된 역지오코딩. 외부 서비스 미설정 시 좌표 유지 |
| 유사 사진·추천 | 구현/알고리즘 검증 완료 | 촬영 시각+시각적 해시, 보수적 그룹, 같은 전처리 내 흔들림/노출/눈 감음 신호 비교. 메타데이터 없으면 동일 원본만 그룹. 샘플 앨범은 후보 없음 상태가 정상 |
| 등록 없는 인물 그룹 | 코드 구현/실연동 미검증 | 앨범별 Rekognition 컬렉션·각 FaceId 검색, 기존 그룹 유지, 연결·병합·분리 및 승인 재검토. fixture에서는 실제 연결 필요 표시 |
| 저장·삭제 정리 | 구현·검증 완료 | 로컬 저장, 비공개 S3 어댑터, 파일 cleanup outbox. 앨범/사진/인물 삭제 화면·API, 삭제 후 선택 목록 정리, 멤버 변경·삭제와 업로드/worker 동시 실행 검증. S3 실연동은 미검증 |
| 실행 구성 | 로컬 검증 완료 | Alembic 0001/0002, lockfile·env·dev.sh·PostgreSQL 스크립트. Docker Compose/Nginx 전체 실행·E2E·DB/API/worker/web 재시작 CI 통과. 실제 AWS 실행은 미검증 |

## 참고 이미지 기준 프론트 재정리

- 데스크톱은 두 번째 이미지의 파란색, 좌측 메뉴·중앙 사진·우측 정보 구성을 적용했다. 검색·앨범 정보·멤버·업로드를 상단에 모으고 사진 격자 간격을 줄였다.
- 모바일은 첫 번째 이미지의 코랄색, 큰 앨범 표지, 인물 아바타, 전체/혼자/함께 필터와 2열 사진을 적용했다. 첫 화면은 앨범 목록이며 사진을 누르면 바로 큰 사진과 보정 화면이 열린다.
- 보정 화면은 큰 사진 → 원본/보정본 전환 → 가로 버전 썸네일 순서다. 확인 요청된 버전은 모바일 사진 아래에 실제 승인 대상·승인 수·승인 버튼을 배치했다. 상세 승인·취소·최종본 지정은 기존 서버 상태와 연결된다.
- 영문 소제목, 감성 문구, 장식 푸터, 반복 안내, 큰 샘플 배너와 앨범 표지 소개 문구를 제거했다. 샘플 여부는 분석 버튼과 상세 기록에서 확인할 수 있다. 삭제/승인에 필요한 확인은 유지했다.
- 현재 설치된 전용 프론트 디자인 스킬은 없었다. Playwright 스킬로 실제 브라우저 화면을 확인하고 기존 통합 테스트를 새 화면에 맞춰 실행했다.

## 실제 실행 결과

- 프론트 `npm --prefix frontend run build`: TypeScript + Vite 프로덕션 빌드 통과.
- 백엔드 전체 **36/36 통과(5.36초)**. 기존 API·분석·이미지 테스트 20개, 새 앨범 관리·동시성 테스트 7개, 연결 점검 명령 테스트 9개.
- Alembic `upgrade head` 및 `check`: 모델과 스키마 일치. 전용 테스트 DB에서 downgrade→upgrade 재적용 확인.
- 프론트 재정리 후 Playwright 데스크톱/모바일 통합 테스트 **3/3 통과(28.6초)**. 업로드·보정·모바일 사진 아래 승인·PC 승인 수 갱신·최종 다운로드·승인 취소, 앨범 관리·재분석·사진 삭제·탈퇴를 검증.
- 최종 데스크톱 1536×1024, 모바일 390×844 및 320×740 화면 육안 확인. 320px 사진 목록의 문서 가로 넘침 없음, 깨진 이미지 0개. 모바일 승인 화면도 실제 두 계정 테스트에서 캡처·확인.
- 새 관리 화면은 데스크톱 1440×1000과 모바일 390px에서 확인. 모바일 정리 현황의 닫기 버튼이 첫 화면에 보이는 조건을 추가한 테스트도 **1/1 통과(3.4초)**. 390×664 화면에서도 제목·닫기와 모달 위치를 직접 확인했다.
- `scripts/verify_restart.py`: 임시 검증 앨범에서 원본·보정본·승인·최종본 생성 → **API와 worker 실제 프로세스 종료/재시작** → 동일 세션·앨범·원본 SHA-256·보정값·승인 수·최종본 유지 확인 → 검증 앨범 삭제. 통과.
- 파일 출처·유통 권리는 [ASSETS.md](ASSETS.md), 실제 AI 미검증 조건은 [AI_VALIDATION.md](AI_VALIDATION.md)에 분리 기록.
- `scripts/check_connections.py --compact`: 종료 코드 0. 현재 fixture/local 설정의 DB·스키마·저장 폴더 접근 권한·샘플 manifest 18건 확인. 로컬 AWS 자격 증명과 S3 버킷 미설정. 실제 AWS 호출/사진 분석/클라우드 쓰기 검증을 통과했다고 해석하지 않는다. [점검 범위](CONNECTIONS.md).

브라우저 테스트는 별도 쿠키 컨텍스트의 두 계정과 고유 테스트 앨범을 사용한다. 앨범 만들기/참여, 얼굴 프로필 연결, 여러 파일 업로드, HEIC 안내, 미등록 fixture 분석 실패, 검색, 서버 미리보기, 버전 저장, 두 명 승인, 최종본 다운로드, 새로고침, 승인 취소와 최종본 해제를 검증한다. 테스트 앨범은 종료 후 정리한다. HTML 리포트와 화면 파일은 `output/playwright/`에 생성한다.

추가 관리 테스트는 소유자와 일반 멤버의 화면 차이, 정보 저장, 초대코드 회전, 실패 재분석 결과, 사진 삭제 확인과 선택 바 해제, 멤버 추방·재가입·탈퇴, 앨범 삭제와 새로고침을 확인한다. API 잠금은 `album → photo → job` 순서로 맞췄으며, 멤버 권한 변경이나 삭제가 업로드·분석 완료와 겹쳐도 늦은 결과나 파일이 남지 않는지 PostgreSQL에서 검증했다. 분석 누적 시간은 저장된 실행의 합계이며 사용자가 기다린 벽시계 시간이 아니다.

## 재현 명령

```bash
bash scripts/postgres-local.sh
TEST_DATABASE_URL=postgresql+psycopg://moacut:moacut-local-only@127.0.0.1:54329/moacut_test .venv/bin/python -m pytest -q backend/tests
.venv/bin/alembic -c backend/alembic.ini check
npm --prefix frontend run build
# 실행 중인 API + worker + frontend에 대해
npm --prefix frontend run test:e2e
```

실제 재시작 검증은 샘플 모드에서 `.venv/bin/python scripts/verify_restart.py setup` 후 API·worker를 재시작하고 `.venv/bin/python scripts/verify_restart.py verify`를 실행한다. 로그인 세션을 담은 임시 상태 파일은 무시되는 `data/`에 권한 0600으로 기록하고 검증 후 삭제한다.

## 남은 조건과 명시적 한계

- 교육용 AWS 콘솔에서 합성 사진의 얼굴 검출·비교는 확인했다. 앱에서 사용할 EC2·S3는 생성하지 않았고 역할 기반 저장·분석, 실제 얼굴 정확도·자동 그룹의 종단 검증은 미실시다. 외부 연결 없이 로컬 파일/DB/보정/수동 인물/승인은 동작한다.
- 로컬에 Docker CLI가 없어 이 기기에서 Compose 검증은 미실시. GitHub Linux runner에서는 Compose 빌드·실행·E2E·재시작 검사가 통과했다. [실행 기록](https://github.com/seopseopi/ZZIK/actions/runs/35166559076).
- `GEOCODING_URL`이 없는 경우 장소명은 추가하지 않는다. 외부 장소 조회 실패는 사진 저장이나 얼굴 분석 결과를 없애지 않는다.
- 한국어 자연어 검색은 제한된 이름/태그/날짜 조합이다. 한국어 이미지 임베딩 기반 의미 검색은 구현하지 않았다(선택 확장).
- 사용자 요청 문서에서 후속 범위로 허용한 OS 푸시, 소셜 직접 게시, 피부/얼굴형 보정, 생성형 합성은 구현하지 않았다.
- 2,000장 성능/정확도/비용 측정, 부하 테스트, 실제 AWS 컬렉션의 DB 장애 중 고아 인덱스 전수 정리는 별도 운영 검증이 필요하다. 검증되지 않은 처리량과 정확도를 UI에서 약속하지 않는다.
- 단순 목적 라벨 `게시 제외`는 앨범 멤버에게 숨기는 권한 기능이 아니다.
- Python 테스트에서 라이브러리의 TestClient/httpx·anyio 폐기 예고 경고 2개가 발생하나 실패는 없다.

후속 사용자 요청으로 `seopseopi/ZZIK` 공개 저장소와 GitHub Pages 체험 사이트를 배포하고 공개 주소에서 확인했다. 체험은 브라우저 내부 저장으로 동작하며 Python·PostgreSQL·AWS 운영 배포와 구분한다. 유료 리소스는 생성하지 않았다. 상세 배포·체험 범위는 [DEMO_DEPLOYMENT.md](DEMO_DEPLOYMENT.md)에 기록한다.

## 팀원 공유용 체험 모드

- `mode=demo`에서만 IndexedDB 기반 체험 API를 사용한다. 일반 실행은 기존 FastAPI에 연결한다. 상단에서 샘플 인물을 전환하고 체험 기록을 초기화할 수 있다.
- 브라우저 보정·버전·승인·다운로드·업로드·초기화·새로고침 유지·방문자별 격리를 브라우저 테스트로 검증한다. 실제 자동 분석·회원가입·원격 협업을 체험 결과로 가장하지 않는다.
- 공개 준비 후 로컬 재검증: 백엔드 **36/36(11.30초)**, 기존 서버 통합 **3/3(39.3초)**, 체험 통합 **2/2**.
- README는 기존 Jipjangbu·XRD_Studio의 중앙 로고·배지·실제 화면·기능표·실행·문서 구조를 참고했다.


## 해커톤 작업 기반 1단계

팀원이 정리한 새 원문은 `docs/hackathon/source/TEAM_SPEC.md`로 보존했다. [팀별 구현·조립 가이드](hackathon/README.md)에 5개 역할과 M01~M17 작업 프롬프트, 공유 파일 담당, 선행 작업, 완료 조건을 작성했다. `plan.json`에서 생성한 프롬프트가 원본과 일치하는지 검사한다.

- `contracts/openapi.json`: API 경로·요청 스키마 스냅샷. 현재 모든 응답에 명시적 response_model이 있는 것은 아니므로 응답 의미/프론트 타입까지 완전히 보증하지 않는다.
- `contracts/python-interfaces.json`: 분석·이미지·저장·그룹·worker의 매개변수 이름 변경 감지. 반환값 의미는 실제 기능 검사로 확인한다.
- `scripts/run_integration.py`: 로컬 전용 `_e2e` DB 안에 임시 스키마를 만들고, 임시 사진 폴더·동적 포트로 API/worker/web을 실행한다. 성공·실패 후 생성한 프로세스와 스키마를 정리한다.
- Vite 프록시와 관리 E2E의 두 번째 계정도 지정한 서버 주소를 사용하도록 연결했다.
- GitHub CI에 `integration` 작업을 추가하고 Pages 배포의 선행 검사에 포함했다. API 테스트와 브라우저 체험 검사도 유지한다.

이번 변경의 로컬 검증: **백엔드 46/46(11.79초)**, **실제 서버 E2E 3/3(41.0초)**, 프론트 빌드, 계약·의존성·프롬프트 검사 통과. 백엔드에는 통합 실행기의 잘못된 DB 대상 거부 검사 10개를 추가했다. 독립 E2E의 마이그레이션·스키마 검사·seed도 통과했다. GitHub CI 결과는 해당 PR의 Checks에 기록한다.

이번 단계는 코드의 도메인 분리, AWS 실검증, 대회용 starter 제작을 완료한 단계가 아니다. 원문 예시인 대비·자르기·웜 필터도 현재 미구현이다. 다음 단계와 완료 기준은 [범위](hackathon/SCOPE.md)와 [서비스 완성 계획](COMPLETION_ROADMAP.md)을 따른다.


## 기능별 구조 분리 2단계

- `main.py`의 기능을 9개 라우터와 dependencies/photo_operations/media로 이동했다. 순환 참조 없이 라우터를 등록하고 공통 트랜잭션·권한 정책은 보존한다. seed도 HTTP 진입점 대신 photo_operations를 사용한다.
- `App.tsx` 내부의 로그인·앨범·갤러리·상세·추천·그룹·보드 등 11개 함수를 9개 기능 컴포넌트 파일로 이동했다. 디자인·DOM·쿼리 키·UI 동작을 유지하고 읽기 쉽게 포맷했다.
- M01~M17 프롬프트의 파일 경로를 갱신했다. 계약 검사에 실제 라우터 구현 파일과 작업 명세의 대응, API 중복 경로 검사를 추가했다. 기존 OpenAPI 스냅샷은 변경하지 않았다.
- 로컬 백엔드 **46/46(8.13초)**, 실제 서버 E2E **3/3(39.5초)**, 체험 E2E **2/2(8.4초)**, 일반·체험 프론트 빌드 통과.
- 독립 실행기에서 API/worker를 실제로 종료·재시작한 뒤 세션·앨범·원본 SHA-256·보정값·승인·최종본 유지와 검증 앨범 정리까지 통과.
- CI에 깨끗한 Docker Compose 빌드, Nginx 경유 E2E, DB/API/worker/web 컨테이너 재시작 검사를 추가했다. backend/frontend/integration/compose **4개 CI 작업 모두 통과**했다. Compose 2분 17초, 직접 실행 integration 1분 51초. [실행 증거](https://github.com/seopseopi/ZZIK/actions/runs/35166559076).

공유 models/services/image_service는 여전히 통합 영역이다. 명시적 응답 모델·프론트 타입 자동 생성, 실제 AWS 검증, starter 제작은 후속 범위다. 컨테이너 재시작 검증은 백업에서의 복원 검증과 구분한다.

## AWS 연결 준비 3단계 — 현재 이어서 작업할 지점

- M01·M03·M05: 합성 PNG 3장, 입력 검증·호출 상한·S3 원본/서명 다운로드·정확한 테스트 객체 정리 기능을 갖춘 `scripts/validate_aws.py`를 준비했다. 콘솔에서의 검출/비교와 앱 역할 기반 검증을 구분한다. [교육 계정 결과](AWS_EDU_VALIDATION.md).
- M01: 앱과 같은 Python 의존성을 사용하는 별도 Docker target과 `build / plan / execute` 명령을 추가했다. `plan`은 네트워크 없이 실행한다. 향후 `execute`는 EC2 자격 증명 공급자와 지정 역할을 먼저 검사한다. [실행 키트](AWS_VALIDATION_CONTAINER.md).
- `a615dcd`의 backend/frontend/integration/compose CI 4개가 통과했다. 백엔드 76개, 실제 컨테이너 빌드·오프라인 plan·기존 앱 E2E·재시작을 포함한다. [실행 기록](https://github.com/seopseopi/ZZIK/actions/runs/35169799177).
- 후속 보완: 보고서 경로 오류를 AWS 호출 전에 거부하도록 수정했다. 관련 로컬 테스트 34개, 하네스·셸 구문 검사를 통과했다. 전체 후속 CI 결과는 [PR #4](https://github.com/seopseopi/ZZIK/pull/4)의 최신 Checks에서 확인한다.
- 사용자 지정 범위는 **신규 AWS 자원 생성 없이 준비까지**다. 실제 EC2 역할·S3·Rekognition 연결, 전체 AWS 배포는 미완료이며 local/fixture 설정을 유지한다. 백업 복원·starter 제작은 이번 단계에서 시작하지 않았다.

현재 개발 브랜치는 `feat/m01-aws-validation-kit`이다. 다음 실제 연결은 실행 환경·버킷·역할 및 비용/유지 시간이 확정된 뒤 진행한다. 이전 단계의 테스트 숫자는 당시 결과이므로 최신 전체 개수와 구분한다.
