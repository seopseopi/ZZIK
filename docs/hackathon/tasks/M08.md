# M08 · 오분류 수동 수정

> `plan.json`에서 생성한 프롬프트입니다. 수정 후 `python scripts/render_hackathon_prompts.py`를 실행하세요.

아래 지시를 작업 프롬프트로 사용한다. 먼저 저장소의 `AGENTS.md`, `docs/hackathon/COMMON.md`, `docs/hackathon/OWNERSHIP.md`, `docs/API_CONTRACT.md`, `contracts/README.md`, `docs/hackathon/PR_WORKFLOW.md`를 읽는다.

- 주관: 3번 백엔드·DB·통합
- 협업: 2번, 4번, 5번
- 우선순위: P0 · 원문 단계: 2
- 선행 작업: M06, M07

## 목표

인물을 직접 추가·제외하고 재분석 및 기존 승인과 일관성을 유지한다.

## 출발 상태

구현·로컬 검증됨. 이 상태는 계획 작성 시점의 기준이며 이번 작업의 완료 증거가 아니다. 기존 구현과 테스트를 읽고 부족한 부분만 변경한다.

## 확인할 파일

- `backend/app/services.py`
- `frontend/src/Editor.tsx`
- `backend/app/routers/photos.py`

## 연결 계약

- `PUT /api/photos/{photo_id}/people`

요청·오류는 `contracts/openapi.json`, 상세 응답 의미는 `docs/API_CONTRACT.md`, Python 호출 경계는 `contracts/python-interfaces.json`을 따른다. 프론트 타입은 OpenAPI에서 생성한다. 계약 변경 시 스냅샷과 타입을 함께 갱신하고 `npm --prefix frontend run types:check` 및 실제 응답 검사를 수행한다.

## 진행 순서

1. `git status`와 선행 PR을 확인하고 M 번호가 있는 작업 브랜치를 사용한다.
2. 현재 기능을 재현하고 완료 조건별로 구현됨/부족함/외부 검증 필요를 나눈다.
3. 주관 범위의 변경을 구현한다. 공유 API·DB·화면 변경은 통합 담당과 조정한다.
4. 아래 검사를 실행하고 정상·실패 경로를 확인한다. 외부 검증은 샘플로 대체하지 않는다.
5. 기능 단위 커밋과 PR에 계약 변경·검사 결과·남은 조건을 기록한다.

## 완료 조건

- [ ] 수동 추가/제외가 재분석 후 유지
- [ ] 다른 앨범 인물 입력 거부
- [ ] 등장 멤버 변경 시 기존 최종본 해제와 재검토

## 검증

`TEST_DATABASE_URL`은 전용 `_test` DB, `ZZIK_E2E_DATABASE_URL`은 로컬 전용 `_e2e` DB를 지정한다. 설치와 예시는 [조립 가이드](../ASSEMBLY.md)를 따른다.

```bash
.venv/bin/python -m pytest -q backend/tests/test_api_integration.py backend/tests/test_album_management.py
.venv/bin/python -m pytest -q backend/tests/test_analysis_worker.py
.venv/bin/python scripts/run_integration.py
.venv/bin/python scripts/check_harness.py
```

## 외부 검증

이 기능을 AWS에서 완료했다고 보고하려면 배포 커밋과 실제 서버 시나리오 결과를 별도로 남긴다.

## 인계 결과

PR 링크, 변경 파일, API/DB 변경 여부, 실행한 검사와 결과, 재현 방법, 다음 담당이 해결해야 할 조건을 짧게 보고한다.
