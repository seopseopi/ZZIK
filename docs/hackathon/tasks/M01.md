# M01 · 3-Tier 기본 연결

> `plan.json`에서 생성한 프롬프트입니다. 수정 후 `python scripts/render_hackathon_prompts.py`를 실행하세요.

아래 지시를 작업 프롬프트로 사용한다. 먼저 저장소의 `AGENTS.md`, `docs/hackathon/COMMON.md`, `docs/hackathon/OWNERSHIP.md`, `docs/API_CONTRACT.md`, `contracts/README.md`, `docs/hackathon/PR_WORKFLOW.md`를 읽는다.

- 주관: 1번 AWS 인프라·배포
- 협업: 2번, 3번, 4번, 5번
- 우선순위: P0 · 원문 단계: 1
- 선행 작업: 공통 계약 확정

## 목표

Web EC2/Nginx → App EC2/FastAPI·별도 worker → RDS PostgreSQL을 분리 배치하고 비공개 S3 사진 저장을 연결한다. 코드 조립과 3-Tier 장애·복구 리허설을 각각 검증해 발표 증거로 남긴다.

## 출발 상태

로컬 DB·사진 백업 복원 및 Docker Compose E2E/재시작, 임시 AWS 인증·합성 사진 분석 함수 실검증됨; 교육/별도 계정 사전 점검 구현, EC2 역할·S3·3-Tier 배포는 미검증. 이 상태는 계획 작성 시점의 기준이며 이번 작업의 완료 증거가 아니다. 기존 구현과 테스트를 읽고 부족한 부분만 변경한다.

## 확인할 파일

- `compose.yaml`
- `infra/nginx.conf`
- `backend/app/db.py`
- `backend/app/main.py`
- `docs/AWS_DEPLOYMENT.md`
- `backend/app/routers/system.py`
- `docs/AWS_EDU_VALIDATION.md`
- `scripts/aws_validation_container.sh`
- `infra/backend.Dockerfile`
- `docs/AWS_VALIDATION_CONTAINER.md`
- `scripts/backup_local.py`
- `scripts/verify_backup_restore.py`
- `docs/BACKUP_RESTORE.md`
- `docs/THREE_TIER_REHEARSAL.md`

## 연결 계약

- `GET /api/health/live`
- `GET /api/health/ready`

요청·오류는 `contracts/openapi.json`, 상세 응답 의미는 `docs/API_CONTRACT.md`, Python 호출 경계는 `contracts/python-interfaces.json`을 따른다. 프론트 타입은 OpenAPI에서 생성한다. 계약 변경 시 스냅샷과 타입을 함께 갱신하고 `npm --prefix frontend run types:check` 및 실제 응답 검사를 수행한다.

## 진행 순서

1. `git status`와 선행 PR을 확인하고 M 번호가 있는 작업 브랜치를 사용한다.
2. 현재 기능을 재현하고 완료 조건별로 구현됨/부족함/외부 검증 필요를 나눈다.
3. 주관 범위의 변경을 구현한다. 공유 API·DB·화면 변경은 통합 담당과 조정한다.
4. 아래 검사를 실행하고 정상·실패 경로를 확인한다. 외부 검증은 샘플로 대체하지 않는다.
5. 기능 단위 커밋과 PR에 계약 변경·검사 결과·남은 조건을 기록한다.

## 완료 조건

- [ ] 새 checkout에서 마이그레이션 후 readiness 통과
- [ ] Web만 외부 진입점이며 App·DB 접근 범위를 문서화
- [ ] AWS 완료는 실제 Web/App/RDS 경유 요청으로 증명; 로컬/Docker 성공과 별도 표시
- [ ] 로컬 DB·사진 백업으로 별도 환경 복원 후 원본/보정본·세션·승인·최종본과 새 업로드 검증; AWS 복원과 구분
- [ ] 별도 Web/App/RDS 실배치 구조도와 배포 커밋·정상 두 계정 흐름 기록
- [ ] 시연 전용 App API/worker 중단 중 Web 정적 자산 응답·오류/재시도 표시와 별도 경로의 RDS/S3 보존 확인
- [ ] 동일 버전 복구 후 세션·파일 해시·설정·승인·최종본 유지 및 신규 업로드·분석 성공; 분석 중 worker 중단 복구도 별도 확인
- [ ] 실행 증거와 미검증 항목을 docs/THREE_TIER_REHEARSAL.md 기준으로 기록; 프로세스 중단과 저장소 손실·고가용성을 구분

## 검증

`TEST_DATABASE_URL`은 전용 `_test` DB, `ZZIK_E2E_DATABASE_URL`은 로컬 전용 `_e2e` DB를 지정한다. 설치와 예시는 [조립 가이드](../ASSEMBLY.md)를 따른다.

```bash
.venv/bin/python scripts/check_harness.py
.venv/bin/python scripts/run_integration.py
.venv/bin/python scripts/verify_backup_restore.py
```

## 외부 검증

계정·리전·예산·리소스 및 HTTPS 설정 필요. 실제 연결 기록과 배포 커밋을 남긴다. 새 자원 생성 보류 유지. 3-Tier 실배치·중단 중 화면·데이터·복구 증거는 별도 확보하며 교육 자료를 대회 규정으로 간주하지 않는다.

## 인계 결과

PR 링크, 변경 파일, API/DB 변경 여부, 실행한 검사와 결과, 재현 방법, 다음 담당이 해결해야 할 조건을 짧게 보고한다.
